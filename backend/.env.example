# Backend için örnek .env dosyası.
# Bu dosyayı .env olarak kopyalayıp gerçek değerleri yazın.
# Bu .env.example git'e gönderilebilir ama .env ASLA commit edilmemelidir.

MONGODB_URI=mongodb://localhost:27017/safe_drive
JWT_SECRET=cok-gizli-bir-anahtar-min-32-karakter-olmali
JWT_EXPIRES_IN=7d
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
BCRYPT_SALT_ROUNDS=10
